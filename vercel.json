{
  "framework": "vite",
  "buildCommand": "cd resilientsa-app && npm install && npm run build",
  "outputDirectory": "resilientsa-app/dist",
  "installCommand": "cd resilientsa-app && npm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
