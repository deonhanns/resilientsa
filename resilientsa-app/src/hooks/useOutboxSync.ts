// src/hooks/useOutboxSync.ts
// Offline-first sync queue — implemented in ORDER 006
// Stub for ORDER 002 scaffold

export function useOutboxSync() {
  // TODO: ORDER 006 — register sync with navigator.serviceWorker, IndexedDB outbox
  return { isSyncing: false, pendingCount: 0 }
}
